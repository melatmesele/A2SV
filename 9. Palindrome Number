class Solution:
    def isPalindrome(self, x: int) -> bool:
        if x < 0:
            return False
        ans = str(x)
        
        ans = ans[::-1]
        
        if int(ans)== x:
            return True
        else:
            return False
